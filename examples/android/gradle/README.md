# Gradle wrapper attribution

The generated `gradlew`, `gradlew.bat`, and `wrapper/gradle-wrapper.jar` are Gradle build tooling, distributed under the included [Apache 2.0 license](LICENSE) and [notice](NOTICE). The repository's original skill and example source remain MIT-licensed.

The wrapper downloads the pinned Gradle distribution on first use. It is not part of the installed agent skill or the demo application's billing behavior.
